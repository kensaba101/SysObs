
// Include common routines
#include <verilated.h>

// Include model header, generated from Verilating "[modulename].v"
#include "Vfiapp.h"


/*
Vfiapp* top = new Vfiapp;
while (!Verilated::gotFinish()) {
    int a = top->v->k;
   int b = top->v->s0->uvw;
   top->eval(); }
exit(0);
*/

 
